# Ralph Board Feature Progress

## FEATURE COMPLETE

All 28 user stories have been implemented. The Ralph Board feature is now complete.

---

## Iteration 28 - US-028: Manual state override with audit trail

### Story Completed
- US-028: Manual state override with audit trail

### Files Changed
- src/lib/managers/ralph-task-manager.ts - Added forceTransitionRalphTask() and getManualOverrideInfo() functions
- src/app/api/ralph/tasks/[taskId]/override-status/route.ts - New endpoint for manual status override
- src/components/dashboard/tabs/RalphBoardTab.tsx - Added "Change Status" dropdown and override confirmation modal

### Implementation Notes
- Created `ManualOverrideInfo` interface with isNonStandard, skippedSteps, and warning fields
- Created `getManualOverrideInfo()` function that:
  - Checks if transition is standard (using isValidTransition)
  - Determines if going backwards in workflow
  - Identifies skipped steps when jumping forward
  - Returns appropriate warning messages
- Created `forceTransitionRalphTask()` function that:
  - Validates reason is required (never empty)
  - Bypasses normal state machine validation
  - Prefixes reason with [Manual Override] for audit trail
  - Handles completedAt timestamp updates when entering/leaving complete state
- Created /api/ralph/tasks/[taskId]/override-status endpoint:
  - GET returns override info for all transitions or specific target status
  - POST executes the override with required reason
- Added "Change Status" dropdown in TaskDetailDrawer footer:
  - Shows all statuses except current status
  - Color-coded status indicators in dropdown
  - Opens override confirmation modal on selection
- Override confirmation modal shows:
  - Current status -> Target status visualization with chips
  - Warning section for non-standard transitions
  - Required reason textarea with description
  - Audit trail notice

### Patterns Discovered
- Override modal nested inside TaskDetailDrawer modal using separate isOverrideModalOpen state
- Select component with onSelectionChange to trigger modal instead of immediate action
- Warning styling uses bg-warning-50 border-warning-200 for attention
- Same callback pattern as other overrides: fetch preview info, open modal, confirm with reason

### Commit
bb84d46 feat: [US-028] Manual state override with audit trail

---

## Iteration 27 - US-027: Promote simple task to feature

### Story Completed
- US-027: Promote simple task to feature

### Files Changed
- src/lib/managers/ralph-task-manager.ts - Added promoteSimpleToFeature() and demoteFeatureToSimple() functions
- src/app/api/ralph/tasks/[taskId]/convert/route.ts - New endpoint for task type conversion
- src/components/dashboard/tabs/RalphBoardTab.tsx - Added convert buttons and confirmation modal

### Implementation Notes
- Created `promoteSimpleToFeature()` function that:
  - Validates task is a simple task in ready state
  - Changes taskType from 'simple' to 'feature'
  - Transitions task to 'planning' state
  - Preserves original prompt as feature description
- Created `demoteFeatureToSimple()` function that:
  - Validates task is a feature with exactly 1 child story in draft state
  - Validates feature is in ready, planning, or research state
  - Deletes the single child story
  - Changes taskType back to 'simple'
  - Clears story generation state from planningState
  - Transitions task to 'ready' state
- Created /api/ralph/tasks/[taskId]/convert endpoint:
  - GET returns preview with canPromote/canDemote flags and action descriptions
  - POST with { action: 'promote' } or { action: 'demote' } executes conversion
- Added `onConvertType` callback prop to TaskCard, KanbanView, ListView
- TaskCard shows "Convert to Feature" button for simple tasks in ready state
- TaskCard shows "Convert to Simple" button for features with 1 draft child
- Conversion confirmation modal shows:
  - Current type -> Target type visualization with chips and arrow
  - Description of what the conversion does
  - Target status the task will move to
  - Warning when demoting (child story will be deleted)
- Modal uses ArrowUpRight icon for promote, ArrowDownRight icon for demote

### Patterns Discovered
- Conditional canPromote/canDemote flags computed in TaskCard based on taskType and status
- Discriminated union schema with `action: z.literal('promote' | 'demote')` for type-safe request handling
- Same callback pattern as other modal flows: openConvertModal handler fetches preview and opens modal
- Demote validation checks child story status (must be draft, not yet worked on)

### Commit
44f1817 feat: [US-027] Promote simple task to feature

---

## Iteration 26 - US-026: Execute simple tasks directly

### Story Completed
- US-026: Execute simple tasks directly

### Files Changed
- src/app/api/ralph/tasks/[taskId]/transition/route.ts - Added special handling for triage->ready transition
- src/components/dashboard/tabs/RalphBoardTab.tsx - Enhanced UI for simple task display

### Implementation Notes
- Simple tasks follow streamlined path: draft -> triage -> ready -> executing -> complete
- When transitioning from triage -> ready ("Proceed as Simple"), taskType is explicitly set to 'simple'
- Planning History section in TaskDetailDrawer shows appropriate message for simple tasks:
  - "Simple task - proceeded directly to execution without planning."
- Completion stats show "Direct execution (no planning needed)" with Zap icon for simple tasks
- List view shows "direct" label for simple tasks with Zap icon
- Simple tasks skip planning phase entirely, using prompt/instructions directly
- All existing simple task flows already worked - these changes make the experience clearer

### Patterns Discovered
- Simple task flow already existed via triage->ready transition with "Proceed as Simple" label
- Task type indicator in UI (Zap icon) helps distinguish simple from feature/story tasks
- Conditional rendering based on taskType==='simple' for appropriate messaging
- updateRalphTask used to ensure taskType is properly set during transition

### Commit
79db02e feat: [US-026] Simple task direct execution flow

---

## Iteration 25 - US-025: User triggers parent completion

### Story Completed
- US-025: User triggers parent completion

### Files Changed
- src/lib/managers/ralph-task-manager.ts - Added feature completion stats and helper functions
- src/app/api/ralph/tasks/[taskId]/complete-feature/route.ts - New endpoint for feature completion
- src/components/dashboard/tabs/RalphBoardTab.tsx - Added feature completion modal and prompt

### Implementation Notes
- Created `FeatureCompletionStats` interface tracking aggregate stats across all child stories
- Created `getFeatureCompletionStats()` function that aggregates:
  - Total/completed/incomplete story counts
  - Total duration, iterations, questions, research iterations across children
  - PR URLs from all children
  - Child summary array with status, duration, PR link for each story
- Created `completeFeatureTask()` function that:
  - Validates task is a feature type
  - Validates all children complete (or force=true with reason)
  - Updates feature with completedAt and optional summary notes
  - Transitions to complete state with appropriate reason
- Created `reopenChildStory()` function that transitions completed story back to ready
- Added `'ready'` to VALID_TRANSITIONS from `'complete'` state to allow reopening
- Feature completion endpoint (GET/POST/PATCH):
  - GET returns preview with stats, child summaries, canComplete status
  - POST completes feature (with optional force and summary notes)
  - PATCH reopens a specific child story with reason
- TaskCard shows "All stories complete - Ready to finalize?" prompt when all children done
- Feature Complete Modal displays:
  - Completion status banner (all complete vs incomplete count)
  - Aggregate stats grid: stories complete, total duration, iterations, PRs created
  - Child stories table with status, duration, PR link, reopen action
  - Reopen story form when user clicks reopen button
  - Summary notes textarea for optional completion notes
  - Force complete checkbox with required reason when children incomplete
  - PRs Created links section

### Patterns Discovered
- Feature completion is manual trigger, not automatic when children finish
- Force completion requires explicit reason to maintain audit trail
- Child story reopening uses same modal context (no separate modal needed)
- Stats aggregation computes fresh from child tasks (no caching)
- exactOptionalPropertyTypes requires `| undefined` suffix for optional function params

### Commit
03f76f5 feat: [US-025] User triggers parent completion

---

## Iteration 24 - US-024: Execute sub-stories individually or in sequence

### Story Completed
- US-024: Execute sub-stories individually or in sequence

### Files Changed
- src/lib/managers/ralph-task-manager.ts - Added batch execution helper functions
- src/app/api/ralph/tasks/[taskId]/batch-execute/route.ts - New endpoint for batch execution
- src/app/api/ralph/tasks/route.ts - Added executionStatus field for feature tasks
- src/components/dashboard/tabs/RalphBoardTab.tsx - Added batch execute buttons and status display

### Implementation Notes
- Added `getReadyChildTasks()` function that returns ready children sorted by storyOrder (respects deps)
- Added `getNextReadyChildTask()` function that returns the first ready unblocked child
- Added `FeatureExecutionStatus` interface tracking totalStories, executingCount, completedCount, readyCount, pendingCount, blockedCount
- Added `getFeatureExecutionStatus()` function that computes status counts for a feature's children
- Created batch-execute endpoint with 'next' and 'all' modes
- POST creates branches and transitions stories to executing (same as individual execute)
- GET returns preview with ready stories and current execution status
- TaskCard now shows execution status panel for features with executing or ready stories
- Shows "Executing X/Y stories" with animated spinner when stories are running
- Shows "N stories ready" with Execute Next and Execute All buttons
- Execute Next button runs next unblocked ready story
- Execute All button runs all ready unblocked stories (shown when >1 ready)
- List view shows inline execution status and batch action buttons
- API response includes executionStatus for all feature tasks with children

### Patterns Discovered
- Batch execution reuses existing branch creation logic from individual execute
- Sequential execution naturally respected via storyOrder and dependency blocking
- Feature execution status computed fresh on each API call (no caching needed)
- Status display split between kanban card body and list view inline/actions

### Commit
339547e feat: [US-024] Execute sub-stories individually or in sequence

---

## Iteration 23 - US-023: Track sub-story dependencies and ordering

### Story Completed
- US-023: Track sub-story dependencies and ordering

### Files Changed
- src/lib/managers/ralph-task-manager.ts - Added blockedBy field and dependency management functions
- src/app/api/ralph/tasks/route.ts - Added blockedBy and isBlocked fields to task response
- src/app/api/ralph/tasks/[taskId]/dependencies/route.ts - New endpoint for dependency CRUD
- src/app/api/ralph/tasks/[taskId]/dependency-graph/route.ts - New endpoint for feature dependency visualization
- src/components/dashboard/tabs/RalphBoardTab.tsx - Added blocked state visualization and disabled execute

### Implementation Notes
- Added `blockedBy: string[]` field to RalphTaskSchema for storing task dependencies
- Added `blockedByCount` to RalphTaskIndexEntry for efficient blocking status queries
- Created `TaskDependencyInfo` interface with blockedBy, resolvedBlockers, activeBlockers, isBlocked, blocks
- Implemented `dfsDetectCycle()` helper function for circular dependency detection using DFS algorithm
- Created `detectCircularDependency()` that validates new blockers won't create cycles
- Created `updateTaskDependencies()` that validates blockers exist and are siblings, then updates
- Created `getTaskDependencyInfo()` that resolves which blockers are complete vs active
- Created `getFeatureDependencyGraph()` that returns full dependency map for feature children
- Created `canTaskExecute()` convenience function that checks if task is blocked
- Dependencies API endpoint supports GET (view deps) and PUT (update deps)
- Dependency graph API returns enriched graph with task titles and statuses
- Task list API now computes isBlocked status by checking blocker completion
- TaskCard shows Lock icon and "Blocked" chip when task is blocked
- Blocked tasks have reduced opacity and warning border styling
- Execute button disabled with Lock icon when task has active blockers
- handleTransitionTask prevents ready->executing transition for blocked tasks

### Patterns Discovered
- DFS helper function extracted to module level to avoid strict mode function-in-block error
- AsyncResult type errors resolved by returning `{ success: false, error: ... }` not casting
- Graph initialization with explicit property setting to satisfy exactOptionalPropertyTypes
- Blocked state computed on API response by checking each blocker's status

### Commit
f2808d6 feat: [US-023] Track sub-story dependencies and ordering

---

## Iteration 22 - US-022: Display nested task cards in board views

### Story Completed
- US-022: Display nested task cards in board views

### Files Changed
- src/app/api/ralph/tasks/route.ts - Added hierarchy fields (parentId, storyOrder, childIds) to API response
- src/components/dashboard/tabs/RalphBoardTab.tsx - Added nested display with expandable parent cards

### Implementation Notes
- Extended RalphTaskResponse with parentId, storyOrder, and childIds fields for hierarchy data
- Added TaskTypeFilter type with options: 'all', 'features', 'stories', 'simple'
- Added showFlat persisted state toggle for flat vs hierarchical view
- Updated TaskCardProps with nested display props: childTasks, isExpanded, onToggleExpand, isNested, depth
- TaskCard now shows:
  - Expand/collapse button for feature tasks with children
  - Task type icons (FolderGit2 for features, BookOpen for stories)
  - Progress bar showing completed/total children (e.g., "5/12")
  - Story order indicator (#N) for nested child cards
  - Indentation and visual connector styling for nested cards
- KanbanView updated with:
  - expandedFeatures state tracking which parents are expanded
  - getChildTasks function to retrieve and sort children by storyOrder
  - Conditional rendering of child TaskCards when parent is expanded
  - In flat mode, shows all tasks including children at root level
- ListView updated with same hierarchical display pattern
- Added Task Type Filter select in header with Layers icon
- Added Flat/Nested toggle button in header with ListTree icon

### Patterns Discovered
- exactOptionalPropertyTypes requires spread operator for conditionally undefined props
- Child tasks sorted by storyOrder field for consistent ordering
- React.Fragment needed for rendering parent + children in loops
- showFlat mode passes tasks to root-level display, nested mode filters to parentId=null

### Commit
644c62a feat: [US-022] Display nested task cards in board views

---

## Iteration 21 - US-021: Generate sub-stories from planning loop with user approval

### Story Completed
- US-021: Generate sub-stories from planning loop with user approval

### Files Changed
- src/lib/managers/ralph-task-manager.ts - Added story parsing and preview management functions
- src/app/api/ralph/tasks/[taskId]/stories/route.ts - New endpoint for story preview CRUD
- src/app/api/ralph/tasks/route.ts - Extended response with story-related fields
- src/components/dashboard/tabs/RalphBoardTab.tsx - Added Stories Preview Modal with inline editing

### Implementation Notes
- Added `GeneratedStoryPreview` type with tempId, title, userStory, acceptanceCriteria, order fields
- Extended `PlanningState` with `generatedStories` array and `storyRevisionFeedback` string
- Created `parseGeneratedStories()` function that extracts US-XXX formatted stories from planning output
- Supports multiple formats: "US-XXX: Title", "### US-XXX", "**US-XXX**", numbered lists
- Extracts acceptance criteria from bullet points, checkboxes, and numbered lists following story
- Created `updateGeneratedStories()` to save edited story previews
- Created `approveGeneratedStories()` that converts previews to child story tasks using `createChildStoryTask()`
- Created `requestStoryRevision()` that stores feedback and transitions task back to planning
- Stories Preview Modal shows all generated stories with inline editing capabilities
- Users can edit title, user story text, and acceptance criteria inline
- Users can delete unwanted stories, add new manual stories, and reorder via up/down buttons
- "Approve & Create Stories" button commits breakdown (creates child tasks)
- "Request Revision" button sends feedback for another planning iteration
- "View Stories" button appears on tasks with hasGeneratedStories=true in kanban/list views

### Patterns Discovered
- Planning output parsing requires flexible regex to handle various markdown formats
- tempId uses `temp-{timestamp}-{random}` pattern for client-side tracking before persistence
- Story approval is atomic: all stories created in single operation, parent updated with childIds
- Revision workflow clears generated stories and stores feedback for next planning iteration

### Commit
f08d8b8 feat: [US-021] Add story generation preview with inline editing

---

## Iteration 20 - US-020: Extend data model for parent/child task hierarchy

### Story Completed
- US-020: Extend data model for parent/child task hierarchy

### Files Changed
- src/lib/managers/ralph-task-manager.ts - Extended task schema with story type and hierarchy fields

### Implementation Notes
- Added `story` task type alongside existing `simple` and `feature` types
- Added `userStory` field for user story format ("As a... I want... so that...")
- Added `acceptanceCriteria` array field for story tasks
- Added `storyOrder` field for ordering stories within a parent feature
- Extended `RalphTaskIndexEntry` with `childCount` and `storyOrder` for efficient hierarchy querying
- Created `createChildStoryTask()` function that:
  - Automatically inherits workspaceId, baseBranch, featureBranch, prTargetBranch from parent
  - Sets parentId reference on the child
  - Updates parent's childIds array
  - Sets parent taskType to 'feature'
- Created `getFeatureProgress()` function that returns { total, completed, inProgress, pending } counts
- Created `reorderChildStories()` function to reorder stories and update their storyOrder field
- Updated `cloneRalphTask()` to copy story-specific fields but reset parent reference

### Patterns Discovered
- Story tasks are children of feature tasks, simple tasks have no children
- Child stories inherit workspace and branch configuration from parent automatically
- storyOrder is 1-based index for display ordering
- Feature tasks have childCount > 0, story tasks have parentId set

### Commit
55f86a4 feat: [US-020] Extend data model for parent/child task hierarchy

---

## Iteration 19 - US-019: Handle completion and history

### Story Completed
- US-019: Handle completion and history

### Files Changed
- src/lib/managers/ralph-task-manager.ts - Added completedAt, isArchived fields and archive/clone functions
- src/app/api/ralph/tasks/[taskId]/archive/route.ts - New endpoint for archiving/unarchiving tasks
- src/app/api/ralph/tasks/[taskId]/clone/route.ts - New endpoint for cloning tasks as templates
- src/app/api/ralph/tasks/[taskId]/stats/route.ts - New endpoint for task completion statistics
- src/app/api/ralph/tasks/route.ts - Added archive filters and completion fields to response
- src/components/dashboard/tabs/RalphBoardTab.tsx - Added completion info display and archive/clone actions

### Implementation Notes
- Added `completedAt` timestamp field to track when task was marked complete
- Added `isArchived` boolean field to support archive view filtering
- Created archive/unarchive endpoints (POST/DELETE on /archive path)
- Created clone endpoint that copies task fields to new draft task
- Created stats endpoint that calculates time in states from transition history
- TaskCard shows expandable completion section for complete tasks
- Completion section shows PR link, completion time, iteration count
- Archive/Unarchive/Clone buttons available on complete tasks
- Added archive toggle button in header to switch between active and archived views
- Tasks API supports archivedOnly and includeArchived query parameters
- calculateTaskStats function computes time spent in each state

### Patterns Discovered
- For optional fields with defaults in zod, need to explicitly exclude from CreateRalphTaskInput Omit
- showArchived toggle triggers fetchTasks refetch via useCallback dependency
- Completion info section follows same expand/collapse pattern as execution progress

### Commit
442a691 feat: [US-019] Add completion handling and history for tasks

---

## Iteration 18 - US-018: Track execution progress

### Story Completed
- US-018: Track execution progress

### Files Changed
- src/lib/managers/ralph-task-manager.ts - Added executionJobId and executionError fields to RalphTask schema
- src/app/api/ralph/tasks/[taskId]/cancel/route.ts - New endpoint for canceling execution
- src/app/api/ralph/tasks/route.ts - Updated to return execution job info
- src/components/dashboard/tabs/RalphBoardTab.tsx - Added execution progress display and cancel functionality

### Implementation Notes
- Added `executionJobId` field to track the associated job when task is executing
- Added `executionError` field to store any execution errors
- Created cancel endpoint that transitions task back to draft and clears execution state
- TaskCard now shows expandable execution progress section for executing tasks
- Execution progress shows job status, job link, start time, and errors if any
- Added cancel button in both kanban and list views for executing tasks
- Tasks API now fetches job info from queue manager for executing tasks
- ExecutionJobInfo interface includes id, status, createdAt, startedAt, completedAt, error
- Progress indicator uses animated Loader2 spinner from lucide-react
- Real-time updates via polling (refetch tasks) - log streaming deferred to future story

### Patterns Discovered
- Job info can be fetched from queue manager using getJob(createJobId(jobId))
- Execution state should be cleared before transitioning to draft on cancel
- Cancel operation is safe - underlying job may continue but task is freed
- ExecutionJobInfo mirrors Job type from queue with appropriate fields

### Commit
ee5967b feat: [US-018] Add execution progress tracking and cancel functionality

---

## Iteration 17 - US-017: Implement Ready state review and execution trigger

### Story Completed
- US-017: Implement Ready state review and execution trigger

### Files Changed
- src/app/api/ralph/tasks/[taskId]/execute/route.ts - Added planInfo to GET preview response
- src/components/dashboard/tabs/RalphBoardTab.tsx - Enhanced execute modal with plan review and back-to-planning
- src/lib/managers/ralph-task-manager.ts - Added `planning` to valid transitions from `ready`

### Implementation Notes
- Added `planning` to VALID_TRANSITIONS from `ready` state (allows sending task back for more planning)
- Added transition label `ready->planning` as "Back to Planning"
- Execute preview endpoint now returns planInfo: { currentPlan, iterationCount, lastOutcome }
- Execute modal enhanced to show expandable plan section before confirming execution
- Added "Back to Planning" button in execute modal footer
- Modal size increased to `lg` with scrollBehavior="inside" for plan content
- New state variables: executePlanInfo, isExecutePlanExpanded
- New handler: handleBackToPlanning - transitions task from ready to planning

### Patterns Discovered
- Plan content can be shown in an expandable section using button with chevron toggle
- Execute modal can serve dual purpose: confirm execution OR return to planning
- ModalFooter can use justify-between layout to put Back to Planning on left, Cancel/Execute on right

### Commit
570c1fa feat: [US-017] Add Ready state review and back-to-planning option

---

## Iteration 16 - US-016: Auto-create PR on task completion

### Story Completed
- US-016: Auto-create PR on task completion

### Files Changed
- src/app/api/ralph/tasks/[taskId]/complete/route.ts - New endpoint for completion/PR creation
- src/components/dashboard/tabs/RalphBoardTab.tsx - Added PR creation modal
- src/lib/managers/ralph-task-manager.ts - Added prUrl field to RalphTask schema

### Implementation Notes
- Created new /api/ralph/tasks/[taskId]/complete endpoint with POST (complete) and GET (preview)
- Complete flow: preview PR info -> user edits title/body -> confirm -> create PR -> mark complete
- Uses existing createPullRequest from lib/github/pull-requests.ts for GitHub
- Uses existing createMergeRequest from lib/gitlab/merge-requests.ts for GitLab
- Provider detection via detectProviderFromUrl from lib/git/provider-detection.ts
- GitHub repo extraction via extractGitHubOwnerRepo
- GitLab project extraction via extractProjectIdFromUrl
- PR title defaults to task title, body auto-generated from task prompt/outcomes
- User can edit title and body before submission
- Option to skip PR creation (just mark complete)
- Option to create as draft PR
- prUrl stored on task record after PR creation
- Modal intercepts executing->complete transition

### Patterns Discovered
- Follow same modal pattern as execute confirmation modal
- Provider detection works for both SSH and HTTPS URLs
- createMergeRequest uses projectId as string (path like "owner/repo")
- createPullRequest uses owner and repo as separate params
- State management pattern: separate loading for preview vs action

### Commit
eb6251e feat: [US-016] Add auto-create PR on task completion

---

## Iteration 15 - US-015: Create feature branch on task execution

### Story Completed
- US-015: Feature branch creation on task execution

### Files Changed
- src/app/api/ralph/tasks/[taskId]/execute/route.ts - New endpoint for execute flow
- src/components/dashboard/tabs/RalphBoardTab.tsx - Added execute confirmation modal

### Implementation Notes
- Created new /api/ralph/tasks/[taskId]/execute endpoint with POST (execute) and GET (preview)
- Execute flow: preview branch info -> user confirmation -> create branch -> transition to executing
- Uses existing switchBranch from lib/git/branches.ts to create feature branch from base
- First switches to base branch, then creates feature branch from HEAD
- Branch name conflicts are detected via getLocalBranches check
- Task updated with baseBranch, featureBranch, prTargetBranch before transition
- Modal shows branch preview with base -> feature arrow visualization
- Modal disables execute button when branch conflict detected
- handleTransitionTask now intercepts ready->executing to open confirmation modal

### Patterns Discovered
- switchBranch can create new branch from current HEAD (checkoutLocalBranch fallback)
- HeroUI Modal with onOpenChange handles cleanup when closing
- Separate loading state for preview vs execute action in modal

### Commit
6cc4cf2 feat: [US-015] Add feature branch creation on task execution

---

## Iteration 14 - US-014: Build branch targeting UI

### Story Completed
- US-014: Branch targeting UI for task form

### Files Changed
- src/app/api/ralph/tasks/[taskId]/route.ts - Added PATCH endpoint for updating task fields
- src/components/dashboard/tabs/RalphBoardTab.tsx - Added editable branch fields in TaskDetailDrawer

### Implementation Notes
- Branch fields (baseBranch, featureBranch, prTargetBranch) were already in the RalphTask data model
- CreateTaskModal already had Git Configuration section with branch inputs
- Added PATCH endpoint with status-based locking (executing/complete statuses block edits)
- TaskDetailDrawer now shows editable branch fields with pencil icon to enter edit mode
- PR target now defaults to selected base branch when base branch changes
- Workspace branches fetched from /api/ralph/workspaces endpoint for dropdowns

### Patterns Discovered
- updateRalphTask(taskId, updates) in ralph-task-manager.ts handles partial updates
- LOCKED_STATUSES pattern used to prevent edits in terminal states
- HeroUI Select component uses selectedKeys as Set<React.Key> requiring Array.from() conversion
- Workspace data includes branches array for branch selection dropdowns

### Commit
de67172 feat: [US-014] Add branch targeting UI for Ralph Board tasks

---

## Previously Completed Stories
- US-001 through US-013 completed in iterations 1-13
